# Utils
mkcd() {
  mkdir "$1" && cd "$1"
}

# Git
alias gs='git status'
alias gps='git push'
alias gpl='git pull'
alias gc='git commit -m'
alias ga='git add'
alias gck='git checkout'
alias gckb='git checkout -b'
alias gb='git branch'

# Docker
alias d='docker'
alias dcu='docker compose up -d --build'
alias dcd='docker compose down'

# Program aliases
alias lzg='lazygit'

# F1tenth
alias mnt_nas='sudo mount -t cifs //192.168.1.1/vaul-nas /mnt/nas -o gid=$(id -g),uid=$(id -u),vers=1.0,user=vaul,pass=macaroni'
